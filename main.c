#include <stdio.h>
#include "bucketSort.h"
int main() {
    int array[]={8,92,305,2,25,32,69,420,5,505};
    int sizeArray = sizeof(array) / sizeof(array[0]);
    Nodo * tablaHash[SIZE];
    printArray(array,sizeArray);
    return 0;
}
